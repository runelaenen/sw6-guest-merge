{% block sw_customer_detail_content_tab_after %}
    {% parent() %}

    <sw-tabs-item
            :route="{ name: 'sw.customer.detail.laeMerge', params: { id: $route.params.id } }"
            :title="$tc('lae-merge.tab.label')"
            v-if="acl.can('customer.editor')">
        {{ $tc('lae-merge.tab.label') }}
    </sw-tabs-item>
{% endblock %}
